name: innermirror
description: A minimal, luxurious journaling app with mirror cards
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  flutter_riverpod: ^2.5.1
  purchases_flutter: ^8.0.0
  speech_to_text: ^7.0.0
  permission_handler: ^11.3.1
  path_provider: ^2.1.1
  http: ^1.2.0
  device_info_plus: ^9.1.1
  # mllama_flutter: ^1.0.0  # Not available on pub.dev - using stub implementation
  workmanager: ^0.5.2
  background_fetch: ^1.1.0
  telephony: ^0.2.0
  image_picker: ^1.0.7
  health: ^10.1.0
  geolocator: ^12.0.0
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  shared_preferences: ^2.2.2
  shake: ^2.0.0
  image: ^4.1.3
  path: ^1.8.3
  flutter_tts: ^4.0.2
  local_auth: ^2.1.7
  flutter_secure_storage: ^9.0.0
  # sqflite: ^2.3.0  # Removed - using file storage instead to avoid native crashes
  audioplayers: ^5.2.1
  audio_waveforms: ^1.0.0
  share_plus: ^7.2.1
  receive_sharing_intent: ^1.7.0
  just_audio: ^0.9.36
  wakelock_plus: ^1.2.0
  archive: ^3.4.9
  cross_file: ^0.3.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.7
  isar_generator: ^3.1.0+1

flutter:
  uses-material-design: true

